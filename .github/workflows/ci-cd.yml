# ============================================
# GitHub Actions CI/CD 流水线配置
# ============================================
# 项目名称: Spring Boot Store Application
# 作者: 陆梦琳
# 日期: 2026-06-26
# 描述: 自动化构建、测试和部署 Spring Boot 应用
# ============================================

name: CI/CD Pipeline for Spring Boot Application

# ============================================
# 触发条件：仅在推送到 main 分支时触发
# ============================================
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

# ============================================
# 环境变量配置
# ============================================
env:
  JAVA_VERSION: '17'
  MYSQL_VERSION: '8.0'
  DOCKER_IMAGE_NAME: 'store-app'
  DOCKER_REGISTRY: 'docker.io'

# ============================================
# 工作流任务定义
# ============================================
jobs:
  
  # ==========================================
  # 阶段1：构建和测试 (Build & Test)
  # ==========================================
  build-and-test:
    name: Build and Test
    runs-on: ubuntu-latest
    
    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: root123
          MYSQL_DATABASE: test_db
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping -h localhost"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=10
    
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Set up JDK ${{ env.JAVA_VERSION }}
        uses: actions/setup-java@v4
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: 'temurin'
          cache: maven
      
      - name: Cache Maven Dependencies
        uses: actions/cache@v4
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-
      
      - name: Wait for MySQL to be ready
        run: |
          echo "Waiting for MySQL service to start..."
          sleep 10
          echo "MySQL should be ready now"
      
      - name: Build and Run Tests
        run: mvn clean verify
        env:
          SPRING_DATASOURCE_URL: jdbc:mysql://localhost:3306/test_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
          SPRING_DATASOURCE_USERNAME: root
          SPRING_DATASOURCE_PASSWORD: root123
      
      - name: Upload Test Reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-reports
          path: |
            target/surefire-reports/
            target/site/jacoco/
          retention-days: 30
      
      - name: Generate Surefire Report
        if: success()
        run: mvn surefire-report:report
      
      - name: Upload Surefire HTML Report
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: surefire-report
          path: target/site/surefire-report.html
          retention-days: 30
  
  # ==========================================
  # 阶段2：构建 Docker 镜像 (Docker Build)
  # ==========================================
  docker-build:
    name: Build Docker Image
    runs-on: ubuntu-latest
    needs: build-and-test
    
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      
      - name: Extract Git Metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.DOCKER_IMAGE_NAME }}
          tags: |
            type=sha,prefix=v,format=long
            type=raw,value=latest
      
      - name: Build Docker Image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: false
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          platforms: linux/amd64
      
      - name: Save Docker Image
        run: |
          docker save ${{ env.DOCKER_IMAGE_NAME }}:latest | gzip > docker-image.tar.gz
      
      - name: Upload Docker Image
        uses: actions/upload-artifact@v4
        with:
          name: docker-image
          path: docker-image.tar.gz
          retention-days: 7